TIL - Today I Learnt Website

Powered by jekyll

commands:
```shell
$ gem install jekyll bundler
$ jekyll new til-website
```

enable `gem "github-pages", group: :jekyll_plugins` in Gemfile
```shell
$ bundle update
```
